<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container px-5">
        <div class="d-inline-flex">
            <a class="nav-link active text-decoration-none text-white " aria-current="page" href="{{ path('course_list') }}">Home</a>
            <a class="nav-link active text-decoration-none text-white" aria-current="page" href="{{ path('new_course') }}">New</a>
            <a class="nav-link active text-decoration-none text-white" aria-current="page" href="{{ path('learning_plan_new') }}">Plan</a>
        </div>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="nav-link position-absolute text-decoration-none text-white"
                   style="right: 120px;" id="openLoginModal">Logout</a>
                <div   class="btn-group ms-3">
                    <button style="right: 30px" type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ app.user.name }}
                    </button>
                    <div class="dropdown-menu dropdown-menu-right">
                        <button class="dropdown-item" type="button">Basic settings</button>
                    </div>
                </div>

                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                        Dropdown button
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                        <li><a class="dropdown-item" href="#">Action</a></li>
                        <li><a class="dropdown-item" href="#">Another action</a></li>
                        <li><a class="dropdown-item" href="#">Something else here</a></li>
                    </ul>
                </div>


{#                <a href="{{ path('app_logout') }}" class="nav-link position-absolute ms-3 text-decoration-none text-white"#}
{#                   style="right: 30px" id="openLoginModal">{{ app.user.name }}</a>#}
            {% else %}
                <a href="{{ path('app_login') }}" class="nav-link position-absolute text-decoration-none text-white"
                   style="right: 150px;" id="openLoginModal">Login</a>
                <a href="{{ path('app_register') }}" class="nav-link position-absolute text-decoration-none text-white"
                   style="right: 50px;"
                   id="openRegisterModal">Register</a>
            {% endif %}
        </div>
    </div>
</nav>